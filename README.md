
* REST calls and SPARQL queries are cached under `$HOME/.cache/mobydex`.

Design:

* Projects: Define a grid. Can list zero or more computations against this grid.
* Computation: Values and metadata for the grid cells of the corresponding project. Computation has a projectId backlink.

