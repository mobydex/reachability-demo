package org.aksw.mobydex.demo.backend.loader;

public class GridComputationService {
    // 1. Fetch the whole project geometry
    // 2. Fetch the individual project cells
    // 3. Intersect project geometry with a geometry source
    // 4. Filter project cells by geometries (from a geometry source)
    // 5. POI-Type-per-Cell Histogram
    // 6. POI-Type-reachability
    // So the point is:
    // - The reachability view first needs the grid cells themselves.
    // - And then the view gets additional attributes for the grid cells.
	
}
